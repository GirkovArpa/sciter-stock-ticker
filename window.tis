function self.ready() {
  const w = 280dip;
  const h = dip(768 / 2);
  const (sw, sh) = view.screenBox(#frame, #dimension);
  view.move((sw / 2) - (w / 2), (sh / 2) - (h / 2), w, h, true);
  view.windowResizable = true;
  view.minSize = (240, 500);
}